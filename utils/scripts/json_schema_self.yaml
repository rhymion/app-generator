$schema: "http://json-schema.org/draft-07/schema#"

definitions:
  procedure:
    type: object
    required:
      - id
      - name
    properties:
      id:
        type: string
        pattern: "^c[a-z0-9]{24,}$"
      name:
        type: string
        minLength: 1
      description:
        type: 
          - string
          - "null"

  procedure_detail:
    x-generate:
      list: true
      view: true
      new: true
      edit: true
      delete: true
      invalidate: false
    x-relationships:
      preceded_by:
        type: many-to-many
        target: procedure
      followed_by:
        type: many-to-many
        target: procedure
    allOf:
      - $ref: "#/definitions/procedure"
      - type: object
        required:
          - preceded_by
          - followed_by
        properties:
          preceded_by:
            type: array
            x-outputType: list
            items:
              $ref: "#/definitions/procedure"
          followed_by:
            type: array
            x-outputType: list
            items:
              $ref: "#/definitions/procedure"
