---
# Single source of truth for local service ports.
# After editing, run: npm run ports:generate
test:
  app: 3000
  postgres:
    host: localhost
    port: 5432
    user: postgres
    password: postgres
    database: my_next_test
  redis:
    host: localhost
    port: 6379
