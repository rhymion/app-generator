/**
 * Virtual column resolver for {{ parent }}.{{ field_name }}.
 * Generated once by generate-code — will NOT be overwritten. Customize freely.
 * Called once per list-page row to populate the virtual '{{ field_name }}' column.
 *
 * @param row - the raw {{ parent_pascal }} record from the database
 * @returns string value to display in the table cell
 */
export function resolve{{ field_pascal }}(row: Record<string, unknown>): string {
  // TODO: replace with real resolver logic
  return '';
}
