// GENERATED ONCE — safe to edit (will not be overwritten on regeneration)
import type { PrismaClient } from '@/app/generated/prisma/client';

type Tx = Omit<PrismaClient, '$connect' | '$disconnect' | '$on' | '$transaction' | '$use' | '$extends'>;

/**
 * Called after {{ parent }} is updated, inside the same Prisma transaction
 * as the write itself. Customize this function to implement post-update
 * side effects.
 *
 * Throwing here rolls back the entire update — update{{ parent_pascal }}()
 * awaits this call from inside its own prisma.$transaction() callback, so
 * an uncaught error propagates out and Prisma rolls back the transaction,
 * including the {{ parent }} row's own update.
 *
 * @param _tx - Prisma transaction client
 * @param _entityId - ID of the updated {{ parent }}
 */
export async function afterUpdate(
  _tx: Tx,
  _entityId: string,
): Promise<void> {
  // TODO: implement post-update effects here
}
