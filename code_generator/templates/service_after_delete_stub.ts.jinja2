// GENERATED ONCE — safe to edit (will not be overwritten on regeneration)
import type { PrismaClient } from '@/app/generated/prisma/client';

type Tx = Omit<PrismaClient, '$connect' | '$disconnect' | '$on' | '$transaction' | '$use' | '$extends'>;

/**
 * Called after {{ parent }} is deleted, inside the same Prisma transaction
 * as the delete itself. Customize this function to implement post-delete
 * side effects.
 *
 * Throwing here rolls back the entire delete — delete{{ parent_pascal }}()
 * awaits this call from inside its own prisma.$transaction() callback, so
 * an uncaught error propagates out and Prisma rolls back the transaction,
 * including the {{ parent }} row's own removal.
 *
 * @param _tx - Prisma transaction client
 * @param _entityId - ID of the deleted {{ parent }}
 */
export async function afterDelete(
  _tx: Tx,
  _entityId: string,
): Promise<void> {
  // TODO: implement post-delete effects here
}
