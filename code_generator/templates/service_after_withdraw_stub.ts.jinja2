// GENERATED ONCE — safe to edit (will not be overwritten on regeneration)

import type { PrismaClient } from '@/app/generated/prisma/client';

type Tx = Omit<PrismaClient, '$connect' | '$disconnect' | '$on' | '$transaction' | '$use' | '$extends'>;

/**
 * Called after {{ snake_name }} is withdrawn.
 * Customize this function to implement post-withdrawal side effects.
 * The `_`-prefixed parameter names below follow this repo's convention for
 * intentionally-unused bindings (see eslint.config.mjs); drop the leading
 * `_` from whichever ones your implementation actually reads.
 *
 * @param _tx - Prisma transaction client
 * @param _entityId - ID of the withdrawn {{ snake_name }}
 * @param _approvableId - ID of the approvable record
 */
export async function afterWithdraw(
  _tx: Tx,
  _entityId: string,
  _approvableId: string,
): Promise<void> {
  // TODO: implement post-withdrawal effects here
}
