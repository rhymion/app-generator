// GENERATED — do not edit (regenerated from all x-scheduled-task
// declarations on every `generate-code` run). One entry per entity that
// declares x-scheduled-task; add a new scheduled task by declaring the key
// on another entity — never by editing this file directly.
{% for e in scheduled_task_entities %}
import { run as {{ e.snake_name | camel_case }}Run } from '@/lib/{{ e.snake_name }}/service_scheduled';
{% endfor %}

export type ScheduledTaskRunner = (systemActorId: string) => Promise<void>;

export const TASK_REGISTRY: Record<string, ScheduledTaskRunner> = {
{% for e in scheduled_task_entities %}
  '{{ e.task_id }}': {{ e.snake_name | camel_case }}Run,
{% endfor %}
};
