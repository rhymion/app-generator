// GENERATED ONCE — safe to edit (will not be overwritten on regeneration)
import type { PrismaClient } from '@/app/generated/prisma/client';

type Tx = Omit<PrismaClient, '$connect' | '$disconnect' | '$on' | '$transaction' | '$use' | '$extends'>;

/**
 * Called before the pending approval_request round for {{ snake_name }} is
 * marked withdrawn (before any approval_request.status is written), inside
 * the same Prisma transaction as that write -- the pre-withdrawal
 * convergence point, symmetric to validateCustomRules() on the create/
 * update side. Customize this function to reject a withdrawal attempt by
 * throwing.
 *
 * Throwing here rolls back the entire withdraw action -- both the REST
 * route (app/api/approval_request/[id]/withdraw/route.ts) and the Server
 * Action path (lib/approval_request/actions_core.ts) await this call from
 * inside their own prisma.$transaction() callback, before any row is
 * written, so an uncaught error prevents the withdrawal entirely.
 *
 * Generated only for entities declaring x-approval.on_withdrawn (cmd_923b):
 * withdrawal itself is blocked upstream (hasOnWithdrawn) for any entity
 * that does NOT declare on_withdrawn, so a stub generated without that
 * condition would be dead code -- never reachable, unlike beforeApprove/
 * beforeReject, which apply to every x-approval entity because approve/
 * reject are always reachable regardless of what an entity's x-approval
 * block declares.
 *
 * Default (unedited) stub is a no-op -- unlike afterWithdraw (opt-in via
 * x-approval.on_withdrawn.emit_hook, a rare side effect), this hook is
 * unconditional within the on_withdrawn-declaring set, matching
 * validateCustomRules's own unconditional default.
 *
 * @param _tx - Prisma transaction client
 * @param _entityId - ID of the {{ snake_name }} about to be withdrawn
 * @param _approvableId - ID of the approvable record
 * @param _actorId - ID of the user performing the withdrawal
 */
export async function beforeWithdraw(
  _tx: Tx,
  _entityId: string,
  _approvableId: string,
  _actorId: string,
): Promise<void> {
  // TODO: implement pre-withdrawal validation here
}
