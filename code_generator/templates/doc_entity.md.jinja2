# {{ title }}

> Auto-generated — run the code generator to update this file. Do not edit manually.

## Overview

| | |
|---|---|
| Entity | `{{ parent }}` |
{% if parent != model %}
| Model | `{{ model }}` |
{% endif %}
| Operations | {{ operations | join(' · ') if operations else '—' }} |
| API | {{ 'Yes — see [API Reference](#api-reference) below' if can_api else 'No' }} |
{% if has_chart %}
| Chart | `{{ chart_span }}` view, grouped by `{{ chart_row_by }}` |
{% endif %}

## Fields

| Field | Type | Required | Read-only | Notes |
|-------|------|:--------:|:---------:|-------|
{% for f in field_rows %}
| `{{ f.name }}` | `{{ f.type }}` | {{ '✓' if f.required else '' }} | {{ '✓' if f.readonly else '' }} | {{ f.notes }} |
{% endfor %}

> **Read-only** fields (`id`, `creator_id`, `created_at`, `updated_at`) are set automatically and cannot be written through the API or UI forms.

{% if is_approvable or write_locks %}
## Constraints

These constraints are entity-level facts, true for every row of this entity regardless of its current data — whether a *specific* row is submittable/locked/transitionable right now depends on that row's own current state and is not shown here.

{% if is_approvable %}
### Approval Flow

This entity is approval-governed (`x-approval`). Every caller — human via the UI, another service via the REST API — goes through the same flow:

| Stage | Sets | Terminal |
|-------|------|:--------:|
{% for step in approval_flow %}
| {{ step.stage }} | {{ step.sets_display }} | {{ '✓' if step.terminal else '' }} |
{% endfor %}

A **terminal** rejection has no resubmission path back to the submit state; a non-terminal one does.

{% endif %}
{% if write_locks %}
### Write-Locked Fields

The listed values may only be written by the system (via the approval decision above, or an explicit `x-write-locked-values` declaration) — an ordinary create/update payload that attempts to set one of these values directly is rejected:

| Field | Locked Values |
|-------|---------------|
{% for lock in write_locks %}
| `{{ lock.field }}` | {{ lock.locked_values | join(', ') }} |
{% endfor %}

{% endif %}
{% endif %}
{% if state_machine_fields %}
## State Machine

{% for sm in state_machine_fields %}
### `{{ sm.field }}`
{% if not can_update %}

> Declared but **not enforced** — this entity has no update path (`Operations` above omits Update), so there is no convergence point for the checks below to run from.
{% endif %}

**States a new row may be created into** (`[*] --> state` in the diagram): every state a new `{{ parent }}` row can land in on creation, not only the state a UI "new" form defaults to — a side-effect hook, a batch/scheduled job, or a data-migration/import path that creates a row is equally legitimate, and each distinct state any of them can produce must be declared here.

{% for s in sm.creation_states %}
- `{{ s }}`
{% endfor %}

**Legal transitions** (checked on update — the value actually submitted must match the *from* state currently on the row):

| From | To |
|------|-----|
{% for from, to in sm.edges %}
| `{{ from }}` | `{{ to }}` |
{% endfor %}

{% if sm.terminal_states %}
**Terminal states** (no outgoing transition; `state --> [*]` in the diagram) — a generate-time diagram-validity marker only ("this state's lack of outgoing edges is intentional"). It has no meaning at runtime and no relationship to delete permission:

{% for s in sm.terminal_states %}
- `{{ s }}`
{% endfor %}
{% endif %}

{% if sm.approval_edges %}
**Further restricted by this entity's Approval Flow (see Constraints above)**: this field is also `x-approval`'s own `submit_on` field, so a transition must be legal in *both* the diagram above *and* the approval flow to actually be permitted right now — the diagram alone is not sufficient:

| From | To |
|------|-----|
{% for from, to in sm.approval_edges %}
| `{{ from }}` | `{{ to }}` |
{% endfor %}
{% endif %}

{% endfor %}
{% endif %}
## Relationships

{% if relations %}
| Field / Property | Target Entity | Relationship Type |
|-----------------|---------------|-------------------|
{% for r in relations %}
| `{{ r.field }}` | [`{{ r.target }}`]({{ r.target }}.md) | {{ r.type }}{% if r.out %} · `{{ r.out }}`{% endif %} |
{% endfor %}
{% else %}
_(none)_
{% endif %}

## UI Pages

| Page | URL | Enabled |
|------|-----|:-------:|
| List | `/en/{{ parent }}` | {{ '✓' if can_list else '✗' }} |
| New | `/en/{{ parent }}/new` | {{ '✓' if can_create else '✗' }} |
| Edit | `/en/{{ parent }}/edit/[id]` | {{ '✓' if can_update else '✗' }} |
| View | `/en/{{ parent }}/view/[id]` | {{ '✓' if can_view else '✗' }} |
{% if has_chart %}
| Chart | `/en/{{ parent }}/chart` | ✓ |
{% endif %}

{% if can_api %}
## API Reference

### Authentication

All endpoints require one of:

| Header | Format | Example |
|--------|--------|---------|
| `X-API-Key` | `<key>` | `X-API-Key: mk_abc123...` |
| `Authorization` | `Bearer <key>` | `Authorization: Bearer mk_abc123...` |

API keys are stored in `user.api_key` and start with the `mk_` prefix.

---

{% if can_list %}
### GET /api/{{ parent }}

List all `{{ title }}` records the authenticated user has permission to read.

**Response `200 OK`**

```json
[
  {{ response_item_json | indent(2) }}
]
```

{% endif %}
{% if can_create %}
### POST /api/{{ parent }}

Create a new `{{ title }}` record.

**Request Body**

```json
{{ request_body_json }}
```

{% if child_body_fields %}
**Child / association fields**

| Field | Type | Description |
|-------|------|-------------|
{% for f in child_body_fields %}
| `{{ f.name }}` | `{{ f.type }}` | {{ f.description }} |
{% endfor %}
{% endif %}

**Response `201 Created`** — the newly created record object.

{% endif %}
{% if can_view %}
### GET /api/{{ parent }}/[id]

Retrieve a single `{{ title }}` record by its ID.

**Response `200 OK`** — the record object (same shape as a list item).

**Response `404 Not Found`**

```json
{ "error": "Not found" }
```

{% endif %}
{% if can_update %}
### PUT /api/{{ parent }}/[id]

Replace an existing `{{ title }}` record. 
{% if can_create %}
Accepts the same body as `POST /api/{{ parent }}`.
{% else %}

**Request Body**

```json
{{ request_body_json }}
```

{% if child_body_fields %}
**Child / association fields**

| Field | Type | Description |
|-------|------|-------------|
{% for f in child_body_fields %}
| `{{ f.name }}` | `{{ f.type }}` | {{ f.description }} |
{% endfor %}
{% endif %}
{% endif %}

**Response `200 OK`**

```json
{ "success": true }
```

**Response `404 Not Found`**

```json
{ "error": "Not found" }
```

{% endif %}
{% if can_delete %}
### DELETE /api/{{ parent }}/[id]

Delete a `{{ title }}` record by its ID.

**Response `204 No Content`** — empty body on success.

**Response `404 Not Found`**

```json
{ "error": "Not found" }
```

{% endif %}

### Error Responses

| Status | Meaning |
|--------|---------|
| `400` | Bad request (validation error) |
| `401` | Missing or invalid API key |
| `403` | Insufficient permission for the operation |
| `404` | Record not found |
| `500` | Unexpected server error |

All error responses return `{ "error": "<message>" }`.

---

## Bulk Operations

Bulk endpoints process each item independently, enabling **partial success**. The outer
request always returns `207 Multi-Status` as long as authentication passes; individual
item outcomes are reported in the `results` array.

### Response shape (`207 Multi-Status`)

```json
{
  "results": [
    { "index": 0, "success": true,  "data": { ... } },
    { "index": 1, "success": false, "error": "Not found: clxxx..." }
  ],
  "summary": { "total": 2, "succeeded": 1, "failed": 1 }
}
```

> Auth/permission failures at the **request** level (bad key, no permission at all) still
> return `401` / `403` — not `207`.
{% if can_create %}

### POST /api/{{ parent }}/bulk

Bulk-create `{{ title }}` records. Each element in the request array is processed with
the same rules as `POST /api/{{ parent }}`.

**Request Body** — array of create objects:

```json
[
  {{ request_body_json | indent(2) }},
  { "..." : "..." }
]
```

**Response `207`** — `data` of each successful item contains `{ "id": "..." }`.
{% endif %}
{% if can_update %}

### PUT /api/{{ parent }}/bulk

Bulk-update `{{ title }}` records. Each element must include `id` in addition to the
fields accepted by `PUT /api/{{ parent }}/[id]`.

**Request Body** — array of update objects:

```json
[
  { "id": "clxxx...", {{ request_body_json[1:] | indent(2) }},
  { "id": "clyyy...", "..." : "..." }
]
```

**Response `207`** — `data` of each successful item is `{ "success": true }`.
Items with an unknown `id` report `"error": "Not found: <id>"`.
{% endif %}
{% if can_delete %}

### DELETE /api/{{ parent }}/bulk

Bulk-delete `{{ title }}` records by ID.

**Request Body** — array of id objects:

```json
[
  { "id": "clxxx..." },
  { "id": "clyyy..." }
]
```

**Response `207`** — `data` of each successful item is `null`.
Items with an unknown `id` report `"error": "Not found: <id>"`.
{% endif %}

{% endif %}
