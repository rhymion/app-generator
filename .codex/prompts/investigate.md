# Investigate - Type C

Treat this as a Type C (investigation) task per `AGENTS.md`.

Rules:

- Do not edit any files.
- Do not run code generation, builds, tests, or docker.
- Read the code with `rg`, `find`, and file reads.
- Answer with `file:line` references where relevant.
- If a proposal would require code changes, present the options with tradeoffs
  but do not implement. Ask whether to proceed; if so, switch to the Type A or
  Type B workflow.

Question:
