{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "agent",
            "prompt": "Verify that all unit tests pass for the code generator python code. Run the code generator, and component test suite. Check the results. Only return ok:true if all of them pass.",
            "timeout": 120
          },
          {
            "type": "prompt",
            "prompt": "Check if all requested tasks are complete and the code change matches the original requirement. If not, respond with {\"ok\": false, \"reason\": \"what remains to be done\"}."
          }
        ]
      }
    ]
  }
}